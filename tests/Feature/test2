<!DOCTYPE html>
<html>
<head>
    <title>Invoice - {{ $record->trxId }}</title>
    <style>
        body { font-family: sans-serif; font-size: 14px; }
        .header { text-align: center; margin-bottom: 30px; }
        .info { width: 100%; margin-bottom: 20px; }
        .table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        .table th, .table td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        .total-section { margin-top: 20px; text-align: right; }
        .status-paid { color: green; font-weight: bold; }
        .status-unpaid { color: red; font-weight: bold; }
    </style>
</head>
<body>
    <div class="header">
        <h2>INVOICE PEMBAYARAN</h2>
        <p>ID Transaksi: <strong>{{ $record->trxId }}</strong></p>
    </div>

    <table class="info">
        <tr>
            <td width="50%">
                <strong>Informasi Pelanggan:</strong><br>
                Nama: {{ $record->name }}<br>
                Email: {{ $record->email }}<br>
                Telp: {{ $record->phone }}
            </td>
            <td width="50%" style="vertical-align: top;">
                <strong>Alamat Pengiriman:</strong><br>
                {{ $record->alamat }}<br>
                {{ $record->kota }}, {{ $record->postCode }}
            </td>
        </tr>
    </table>

    <table class="table">
        <thead>
            <tr style="background-color: #f2f2f2;">
                <th>Produk (ID: {{ $record->shoe_id }})</th>
                <th>Ukuran</th>
                <th>Jumlah</th>
                <th>Subtotal</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>Sepatu (Shoe ID: {{ $record->shoe_id }})</td>
                <td>{{ $record->size }}</td>
                <td>{{ $record->jumlah }}</td>
                <td>Rp {{ number_format($record->subTotal_amount, 0, ',', '.') }}</td>
            </tr>
        </tbody>
    </table>

    <div class="total-section">
        <p>Promo Code ID: {{ $record->promo_code_id ?? '-' }}</p>
        <h3>Grand Total: Rp {{ number_format($record->grandTotal_amount, 0, ',', '.') }}</h3>
        <p>Status:
            <span class="{{ $record->isPaid ? 'status-paid' : 'status-unpaid' }}">
                {{ $record->isPaid ? 'LUNAS' : 'PENDING' }}
            </span>
        </p>
    </div>
</body>
</html>
